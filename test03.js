// operater

// Aritamic
// + - * / % **

// increament เพิ่อตัวเองทีละ 1, Decreament ลดตัวเองทีละ 1
// ++, --

// Comparison
// > < >= <= == !==   ไม่คำนึงถึงชนิดข้อมูล
// === !== คำนึงถึงชนิดข้อมูล
console.log(10 == '10'); //True
console.log(10 === '10');  //False

// Loical
// && || !

// Nullish Coalescing
// ?? ดูว่าเครื่องหมาย เป็น null หรือ undefined หรือไม่
// ??
let x = null
let y = 20
let z =
console.log(x ?? 'Hello');
console.log(y ?? 'Hii');
console.log(z ?? 'Heyy');

// Ternary Operater
// เงื่อนไข ? ทำ/ได้เมื่อเงื่อนไขเป็นจริง  :  ทำ/ได้เมื่อเงื่อนไขเป็นจริง
// conditinn ? true : false
console.log(10< 20 ? 'Hello' : 'Hii')
console.log(100 < 200 ? 'wow' : 200 < 300 ? 'hi' : 'mint');

// Asingment Oparater
// = += -= *= /= %= **=
